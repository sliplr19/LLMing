% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_datagen.R
\name{text_datagen}
\alias{text_datagen}
\title{Generate Synthetic Text Using an Ollama Language Model}
\usage{
text_datagen(
  prompt_info_csv,
  examples_csv,
  output_csv,
  model,
  system_prompt,
  prompt_template,
  items,
  severity_instructions,
  python_path = "python",
  severity_min = 10,
  severity_max = 90,
  label_order = c("minimum", "moderate", "severe"),
  label_order_double = c("minimum", "moderate", "severe", "severe", "moderate",
    "minimum"),
  batch_size = 2L,
  max_retries = 2L,
  temperature = 0.7,
  top_p = 0.9,
  repeat_penalty = 1.05,
  num_predict = 1200L,
  num_ctx = 8192L,
  min_words = 80L,
  max_words = 400L,
  require_single_paragraph = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{prompt_info_csv}{Character string. Path to the CSV containing the
generation conditions. The file must contain a \code{severity} column and may
contain \code{num} and \code{seed} columns. \code{num} controls the number of examples
selected from each example category, and \code{seed} controls random example
selection.}

\item{examples_csv}{Character string. Path to the CSV containing few-shot
text examples. The file must contain \code{label} and \code{text} columns.}

\item{output_csv}{Character string. Path where the CSV containing the
generated text will be written.}

\item{model}{Character string. Name of the Ollama model used for text
generation, for example \code{"llama3:8b"}.}

\item{system_prompt}{Character string. System-level instruction supplied
to the language model.}

\item{prompt_template}{Character string. Template used to construct the
user prompt. The Python worker should replace \code{{severity}},
\code{{severity_instructions}}, \code{{items}}, and \code{{example_section}} with the
corresponding information for each generation.}

\item{items}{Character string. Questionnaire items, construct description,
scoring information, or other measurement information supplied to the
language model.}

\item{severity_instructions}{A data.frame with columns \code{min}, \code{max}, and
\code{instructions}. Each row defines the instructions associated with a
range of severity values.}

\item{python_path}{Character string. Python executable used to run the
package's Python generation script. Defaults to \code{"python"}.}

\item{severity_min}{Numeric. Minimum valid severity value. Defaults to 10.}

\item{severity_max}{Numeric. Maximum valid severity value. Defaults to 90.}

\item{label_order}{Character vector. Order in which example categories are
selected when \code{num = 1}.}

\item{label_order_double}{Character vector. Order in which example
categories are selected when \code{num = 2}.}

\item{batch_size}{Integer. Batch-size argument supplied to the Python
generation script. Defaults to 2.}

\item{max_retries}{Integer. Number of retries permitted after an invalid
or failed model response. Defaults to 2.}

\item{temperature}{Numeric. Ollama sampling temperature. Higher values
generally produce more variable output. Defaults to 0.7.}

\item{top_p}{Numeric. Nucleus-sampling probability passed to Ollama.
Defaults to 0.9.}

\item{repeat_penalty}{Numeric. Repetition penalty passed to Ollama.
Defaults to 1.05.}

\item{num_predict}{Integer. Maximum number of tokens that Ollama may
generate for each response. Defaults to 1200.}

\item{num_ctx}{Integer. Context-window size supplied to Ollama. Defaults
to 8192.}

\item{min_words}{Integer. Minimum number of words required for a generated
response to be considered valid. Defaults to 80.}

\item{max_words}{Integer. Maximum number of words permitted for a generated
response to be considered valid. Defaults to 400.}

\item{require_single_paragraph}{Logical. If \code{TRUE}, responses containing
multiple paragraphs are considered invalid. Defaults to \code{TRUE}.}

\item{verbose}{Logical. If \code{TRUE}, output from the Python subprocess is
printed to the R console. Defaults to \code{TRUE}.}
}
\value{
A data.frame containing the original generation-condition columns
and a \code{response} column containing the generated text.
}
\description{
Generates synthetic text using a locally available Ollama language model.
Generation conditions are supplied in an input CSV, and few-shot examples
are supplied in a separate example CSV. Prompt content, severity
instructions, model settings, and generation settings can be customized.
}
\details{
The Python generation script \code{text_datagen.py} is included with the package
and is located automatically. Users do not need to provide the path to the
Python script.
}
