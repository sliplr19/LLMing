% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{embed}
\alias{embed}
\title{Generate document embeddings}
\usage{
embed(
  dat,
  embed_method = c("Qwen", "NV", "e5", "word2vec"),
  text_col,
  batch_size = NULL,
  python_path = NULL,
  cache_dir = NULL,
  model = NULL,
  python_script = "embed.py",
  clean = TRUE,
  word2vec_dim = 50L,
  word2vec_iter = 20L,
  word2vec_window = 5L,
  word2vec_threads = 1L,
  verbose = TRUE
)
}
\arguments{
\item{dat}{Dataframe containing the text column.}

\item{embed_method}{Embedding method: "Qwen", "NV", "e5", or "word2vec".}

\item{text_col}{Name of the text column in dat.}

\item{batch_size}{Positive integer batch size. If NULL, method-specific
defaults are used.}

\item{python_path}{Path to a Python executable. If NULL, searches for
"python3" or "python". Ignored for word2vec.}

\item{cache_dir}{Hugging Face cache directory. If NULL, uses the package
user cache directory. Ignored for word2vec.}

\item{model}{Optional Hugging Face model ID. If NULL, uses the package
default for the selected embedding method.}

\item{python_script}{Path to Python embedding script. Defaults to package script.}

\item{clean}{Logical. If TRUE, apply the package's \code{clean_texts()} function
before embedding.}

\item{word2vec_dim}{Embedding dimension for word2vec.}

\item{word2vec_iter}{Number of word2vec training iterations.}

\item{word2vec_window}{Context window for word2vec.}

\item{word2vec_threads}{Number of threads for word2vec.}

\item{verbose}{Logical. Print Python subprocess output.}
}
\value{
A dataframe with one row per input row and one column per
embedding dimension.
}
\description{
Generate one embedding vector per row of a data frame using Qwen,
NV-Embed, E5, or a locally trained word2vec model.
}
